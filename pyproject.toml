[project]
name = "djbot-app-1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django-bolt>=0.9.0",
]

[tool.ruff.lint.flake8-bugbear]
extend-immutable-calls = ["django_bolt.params.Depends"]
